---
interface Props {
    id: string;
    name: string;
    img: string;
    success: boolean;
    flightNumber: number;
    details: string;
}
const { img }: Props = Astro.props;
---
</header>
<picture>
    <img class="h-auto max-w-full rounded-lg " src={img} />
</picture>

